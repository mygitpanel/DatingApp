/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HasRoleDirectiveDirective } from './hasRole-directive.directive';

describe('Directive: HasRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new HasRoleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
